DROP TABLE IF EXISTS hits;

CREATE TABLE IF NOT EXISTS hits (
  hits_id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  hits_app VARCHAR(64) NOT NULL,
  hits_uri VARCHAR(256) NOT NULL,
  hits_ip VARCHAR(16) NOT NULL,
  hits_timestamp TIMESTAMP WITHOUT TIME ZONE NOT NULL,
  CONSTRAINT pk_hit PRIMARY KEY (hits_id)
);